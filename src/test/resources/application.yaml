spring:
  datasource:
    url: jdbc:postgresql://192.168.1.8:5432/postgres?currentSchema=blogs_test
    username: postgres
    password: Root@1234
  jpa:
    hibernate:
      ddl-auto: none
    defer-datasource-initialization: true
  sql:
    init:
      mode: always